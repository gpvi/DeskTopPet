# 验收与质量门禁

## 1. 静态门禁

每次任务进入 `review` 前至少运行与变更相关的门禁。

| 命令 | 用途 | 必需场景 |
| --- | --- | --- |
| `npm run lint` | TypeScript/React 静态检查 | 前端、应用层、脚本变更 |
| `npm run test:run` | 单元测试 | 用例、Repository、Controller、runtime 变更 |
| `npm run build` | 前端生产构建 | UI、tsconfig、Vite、依赖变更 |
| `npm run smoke:check` | MVP 主链路静态自检 | 文档路径、任务链路、QuickActions、Controller 变更 |
| `npm run tauri:check:gnu` | Tauri GNU 编译检查 | `src-tauri`、权限、插件、dev 脚本变更 |

## 2. MVP GUI 验收清单

| 能力 | 验收项 | 当前状态 |
| --- | --- | --- |
| DEV008 对话主链路 | 打开聊天面板，输入普通消息，收到助手回复 | 待 GUI 终验 |
| 会话持久化 | 用户消息和助手回复刷新/重启后可读取 | 待 GUI 终验 |
| DEV012 提醒 | 自然语言创建提醒，到点触发通知或面板反馈 | 待 GUI 终验 |
| DEV013 待办 | 创建、查看、完成、重启后保留 | 待 GUI 终验 |
| DEV014 剪贴板 | 总结/改写/翻译剪贴板文本，空剪贴板有提示 | 待 GUI 终验 |
| DEV015 打开工具 | 打开 URL、应用、文件夹，并回显结果 | 待 GUI 终验 |
| DEV019 动效 | idle/thinking/happy/reminder 状态可切换 | 待 GUI 终验 |
| 设置 | 模型、通知、自启动、快捷键设置可保存 | 待 GUI 终验 |
| 权限 | Tauri 权限缺失时有可理解反馈 | 待 GUI 终验 |

## 3. 缺陷分级

| 等级 | 定义 | 示例 |
| --- | --- | --- |
| P0 | 阻断主路径或数据安全 | 无法启动、无法聊天、构建失败、数据丢失 |
| P1 | 主功能可用但体验或可靠性明显受损 | 提醒重复触发、权限提示不清、快捷键冲突 |
| P2 | 优化项 | 文案、布局、动效细节、日志格式 |

## 4. 验收记录模板

```text
验收日期：
执行人：
环境：
关联任务：
关联提交：

静态门禁：
- lint:
- test:
- build:
- smoke:
- tauri check:

GUI 验收：
- 对话：
- 提醒：
- 待办：
- 剪贴板：
- 打开工具：
- 动效：
- 设置与权限：

阻塞与风险：
后续任务：
结论：pass/fail/blocked
```

## 5. 当前已知阻塞

- `npm run build` 当前受 TypeScript 6 `baseUrl` 弃用配置影响，见 `BUG012`。
- `npm run tauri:check:gnu` 可能被残留 `app.exe/cargo/node` 占用，见 `BUG013`。
- `DEV025` 和 `TEST009` 尚未完成 GUI 全链路验收。

## 6. done 判定

任务只能在满足以下条件后改为 `done`：

- 代码变更已完成且无明显架构越界。
- 相关测试或人工验收已执行。
- 文档、任务状态、验收记录已同步。
- 若存在未解决风险，必须拆出新的任务并标明优先级。
