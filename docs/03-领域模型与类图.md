# 领域模型与类图

## 1. 核心实体

| 实体 | 责任 | 关键字段 |
| --- | --- | --- |
| `UserProfile` | 用户基础资料和偏好 | `userId`、`nickname`、`privacySettings` |
| `PetPersona` | 桌宠人格和语气规则 | `personaId`、`name`、`traits`、`toneRules` |
| `ConversationSession` | 一次会话上下文 | `sessionId`、`userId`、`startedAt`、`lastActiveAt` |
| `ConversationMessage` | 用户/助手消息 | `messageId`、`sessionId`、`role`、`content` |
| `AgentTask` | 可追踪任务单元 | `taskId`、`intent`、`status`、`parameters` |
| `ToolExecution` | 具体工具调用记录 | `executionId`、`taskId`、`toolName`、`status` |
| `Reminder` | 提醒任务 | `reminderId`、`scheduleRule`、`status` |
| `TodoItem` | 待办事项 | `todoId`、`title`、`status`、`dueAt` |
| `UserMemory` | 显式长期记忆 | `memoryId`、`category`、`content`、`confidence` |
| `ModelUsageRecord` | 模型调用和 Token 统计 | `usageId`、`feature`、`provider`、`tokens` |

## 2. 领域类图

```mermaid
classDiagram
  class UserProfile {
    +string userId
    +string nickname
    +string preferredTone
    +PrivacySettings privacySettings
  }

  class PetPersona {
    +string personaId
    +string name
    +string[] traits
    +string[] toneRules
  }

  class ConversationSession {
    +string sessionId
    +string userId
    +Date startedAt
    +Date lastActiveAt
  }

  class ConversationMessage {
    +string messageId
    +string sessionId
    +MessageRole role
    +string content
    +Date createdAt
  }

  class AgentTask {
    +string taskId
    +string userId
    +TaskIntent intent
    +TaskStatus status
    +Record parameters
  }

  class ToolExecution {
    +string executionId
    +string taskId
    +string toolName
    +ExecutionStatus status
    +string resultSummary
  }

  class Reminder {
    +string reminderId
    +string userId
    +string title
    +ScheduleRule scheduleRule
    +ReminderStatus status
  }

  class TodoItem {
    +string todoId
    +string userId
    +string title
    +TodoStatus status
    +Date dueAt
  }

  class UserMemory {
    +string memoryId
    +string userId
    +string category
    +string content
    +number confidence
  }

  class ModelUsageRecord {
    +string usageId
    +string userId
    +string feature
    +string provider
    +number inputTokens
    +number outputTokens
  }

  UserProfile "1" --> "*" ConversationSession
  ConversationSession "1" --> "*" ConversationMessage
  UserProfile "1" --> "*" UserMemory
  UserProfile "1" --> "*" Reminder
  UserProfile "1" --> "*" TodoItem
  ConversationSession "1" --> "*" AgentTask
  AgentTask "1" --> "*" ToolExecution
  AgentTask "1" --> "*" ModelUsageRecord
  PetPersona ..> ConversationMessage : shapes replies
```

## 3. 端口与实现类图

```mermaid
classDiagram
  class LLMGateway {
    <<interface>>
    +completeChat(request)
    +classifyIntent(request)
    +generateReminderCopy(request)
    +generateCompanionReply(request)
  }

  class ToolExecutor {
    <<interface>>
    +openUrl(input)
    +openApplication(input)
    +openFolder(input)
    +readClipboard()
    +showNotification(input)
  }

  class ConversationRepository {
    <<interface>>
    +saveSession(session)
    +saveMessage(message)
    +findMessages(sessionId)
  }

  class MemoryRepository {
    <<interface>>
    +save(memory)
    +findRelevant(query)
    +delete(memoryId)
    +clearByUser(userId)
  }

  class UsageRepository {
    <<interface>>
    +save(record)
    +queryDailyTrend(filter)
    +aggregateByDimension(filter)
  }

  class LlmGatewayImpl
  class OpenAICompatibleProvider
  class TauriToolExecutor
  class WebToolExecutor
  class SqliteConversationRepository
  class SqliteMemoryRepository
  class SqliteUsageRepository

  LLMGateway <|.. LlmGatewayImpl
  LlmGatewayImpl --> OpenAICompatibleProvider
  ToolExecutor <|.. TauriToolExecutor
  ToolExecutor <|.. WebToolExecutor
  ConversationRepository <|.. SqliteConversationRepository
  MemoryRepository <|.. SqliteMemoryRepository
  UsageRepository <|.. SqliteUsageRepository
```

## 4. 用例依赖图

```mermaid
flowchart LR
  UI["React UI"] --> Store["chat/settings stores"]
  Store --> Controller["ConversationController"]
  Controller --> Classify["ClassifyIntentUseCase"]
  Controller --> Send["SendMessageUseCase"]
  Controller --> Reminder["CreateReminderUseCase"]
  Controller --> Todo["ManageTodoUseCase"]
  Controller --> Clipboard["ClipboardUseCase"]
  Controller --> OpenTool["OpenToolUseCase"]
  Controller --> Memory["Save/RecallMemoryUseCase"]

  Classify --> LLM["LLMGateway"]
  Send --> LLM
  Clipboard --> LLM
  Reminder --> Repo["SQLite repositories"]
  Todo --> Repo
  Memory --> Repo
  OpenTool --> Tool["ToolExecutor"]
  Clipboard --> Tool
```

## 5. 建模注意事项

- `AgentTask` 是任务可观测性的中心，后续应补齐任务表或任务日志。
- `ToolExecution` 不等同于 `AgentTask`，一个任务可以包含多次工具调用。
- `UserMemory` 只保存显式记忆，不应由聊天历史自动无限写入。
- `ModelUsageRecord` 应覆盖聊天、意图识别、提醒文案、剪贴板处理等所有模型调用。

